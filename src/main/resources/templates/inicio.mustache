{{! index.mustache }}
{{<base}}
{{$header}}
{{<header}}
{{$title}}PayFact! | Inicio{{/title}}
{{$links}}
<link rel="stylesheet" type="text/css" href="/css/Cliente.css">   
{{/links}}
{{/header}}
{{/header}}
{{$content}}
{{$navbar}}
{{<navbar}}
{{/navbar}}
{{/navbar}}

<div class="container">
    <div class="text-left">
         <h1 class="">Clientes</h1>

    </div>
    <div class="col-sm-5 col-md-5 pull-left">
        <div class="navbar-text">Cliente</div>
        <form class="navbar-form" role="search">
            <div class="input-group">
                <input type="text" class="form-control" placeholder="Search" name="srch-term"
                id="srch-term">
                <div class="input-group-btn">
                    <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i>

                    </button>
                </div>
            </div>
        </form>
        <form class="navbar-form" role="search">
            <div class="input-group">
                <div class="input-group-btn"></div>
            </div>
        </form>
    </div>
    <table class="table table-bordered">
        <thead>
            <tr>
              <th>Cedula</th>
              <th>Nombre</th>
              <th>Apellido</th>
              <th>Dirección</th>
              <th>Telefono #1:</th>
              <th>Telefono #2:</th>
            </tr>
        </thead>
        <tbody>
            {{#clientes}}
            <tr>
              <td>{{cedulaidentidad}}</td>
              <td>{{nombre}}</td>
              <td>{{apellido}}</td>
              <td>{{direccion}}</td>
              <td>{{telefono1}}</td>
              <td>{{telefono2}}</td>
              <td><a href="/inicio/{{tipo}}/clientes/{{idcliente}}" class="btn btn-primary">Edit</a></td>
            </tr>
            {{/clientes}}
        </tbody>
    </table>
</div>
{{/content}}
{{/base}}